version: '3'

includes:
  generate: ./tasks.d/generate.yaml
  hugo: ./tasks.d/hugo.yaml
  python: ./tasks.d/python.yaml

env:
  MAKEFLAGS: '--no-print-directory'
  TASK_TEMP_DIR: .cache/task

vars:
  TOOL_DIR: .tool

  LYCHEE: '{{ .TOOL_DIR }}/lychee'

tasks:
  ## Bootstrap tasks #################################

  binny:
    internal: true
    generates:
      - '{{ .TOOL_DIR }}/binny'
    status:
      - 'test -f {{ .TOOL_DIR }}/binny'
    cmd: 'curl -sSfL https://get.anchore.io/binny | sh -s -- -b .tool'
    silent: true

  tools:
    desc: Install all tools needed for CI and local development
    deps: [binny]
    aliases:
      - bootstrap
    generates:
      - '.binny.yaml'
      - '{{ .TOOL_DIR }}/*'
    status:
      - '{{ .TOOL_DIR }}/binny check -v'
    cmd: '{{ .TOOL_DIR }}/binny install -v'
    silent: true

  update-tools:
    desc: Update pinned versions of all tools to their latest available versions
    deps: [binny]
    generates:
      - '.binny.yaml'
      - '{{ .TOOL_DIR }}/*'
    cmd: '{{ .TOOL_DIR }}/binny update -v'
    silent: true

  list-tools:
    desc: List all tools needed for CI and local development
    deps: [binny]
    cmd: '{{ .TOOL_DIR }}/binny list'
    silent: true

  list-tool-updates:
    desc: List all tools that are not up to date relative to the binny config
    deps: [binny]
    cmd: '{{ .TOOL_DIR }}/binny list --updates'
    silent: true

  # high level tasks

  install:
    desc: Install all development dependencies
    cmds:
      - task: hugo:install
      - task: python:install

  dev:
    desc: Start development environment with live reload
    deps:
      - install
    cmds:
      - task: hugo:dev

  default:
    desc: Default task (validate)
    cmds:
      - task: validate

  lint:
    desc: Run all linters
    cmds:
      - task: python:lint
      - task: hugo:lint

  lint-fix:
    desc: Auto-fix linting issues where possible
    cmds:
      # python: format -> fix/infer -> format again (pyrefly may break formatting)
      - task: python:format
      - task: python:lint-fix
      - task: python:format
      # hugo/node: prettier --write + markdownlint --fix
      - task: hugo:lint-fix
      # universal whitespace fixers (must match what pre-commit checks)
      # note: --hook-stage push is required because these hooks are configured for pre-push stage
      - uv run pre-commit run end-of-file-fixer --all-files --hook-stage push || true
      - uv run pre-commit run trailing-whitespace --all-files --hook-stage push || true

  format:
    desc: Format all code
    cmds:
      - task: python:format

  test:
    desc: Run all tests
    cmds:
      - task: unit
      - task: e2e

  unit:
    desc: Run all unit tests
    cmds:
      - task: python:unit

  e2e:
    desc: Run all end-to-end tests
    cmds:
      - npm test

  validate:
    desc: Run all validation tasks
    cmds:
      # run linters for high and low level concerns across stacks
      - uv run pre-commit run -a --hook-stage push
      # per-stack tests
      - task: python:test
      - task: hugo:test

  # note: this target name cannot be changed, as it is used by cloudflare pages to build and deploy the site
  release:
    desc: Build site for production release
    deps: [tools]
    cmds:
      - npm install
      - npm run build
